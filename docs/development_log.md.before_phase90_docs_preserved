# ehp_proof 開発記録

現在の仕様は `README.md` / `docs/design.md` を優先する。

この文書は chronological implementation history として運用する。
過去の Phase の意味を後から書き換えず、現在の理解に照らして決定的に誤っている説明のみ訂正し、新しい Phase は原則として末尾へ追記する。

---

# Phase 1–27 概要

可換群計算、汎用推論、EHP、ORDER、Suspension、Freudenthal、Composition、Hopf 不変量、加法、準同型、部分群、modulo、記号的 scalar、不定性推論、unstable Toda bracket、型付き要素、生成元 fact を整備。

### 状態

COMPLETE

---

# Phase 28–38 概要

Toda Proposition 2.2、Barratt–Hilton、actual H branch を接続し:

```text
H((2ι₂)η₂)=4ι₃
H(4η₂)=4ι₃
↓
H((2ι₂)η₂)=H(4η₂)
↓
Injective(H)
↓
(2ι₂)η₂=4η₂
```

まで完成。

### 状態

COMPLETE

---

# Phase 39–48 概要

```text
39 PrimaryComponent
40 TodaPrimaryGroup
41 PreimageSubgroup
42 WhiteheadProduct
43 Toda Lemma 4.1 premise representation
44 Toda Lemma 4.1 case semantics
45 Toda Proposition 4.2 EHP exactness
46 Toda (4.5) stable-range isomorphism
47 Toda Proposition 4.4 decomposition
48 Toda Proposition 4.4 E injectivity
```

### 状態

COMPLETE

---

# Phase 49–55：η-family / Proposition 5.1

主要結果:

```text
π_3^2=Z{η₂}
π_4^3=Z/2{η₃}
Δ(ι₅)=±2η₂
π_(n+1)^n=Z/2{η_n}
Toda Proposition 5.1 finite-dimensional integration
```

Phase 55 の最終 aggregate は4つの derived premise を `ProofRule.INFERENCE` として要求する。

### 状態

COMPLETE

---

# Phase 56：Toda (5.2)

目標:

```text
η₂∘- : π_i^3 ≅ π_i^2
(i≥3)
```

Proposition 4.4 の n=2 specialization と second-summand restriction を接続。

### 状態

COMPLETE

---

# Phase 57：Toda Lemma 5.2

対象:

```text
α∈π_i(S³)
2α=0
β∈{η₃,2ι₄,Eα}_1
```

導出:

```text
H(β)=E²α
2β=η₃∘Eα∘η_(i+1)
β∈π_(i+2)^3
Δ(E²α)=0
```

Lemma 4.5、Proposition 2.6、Proposition 1.4、Proposition 1.3、Corollary 3.7、Toda (2.1) の必要最小限の consequence を theorem-specific に実装。

### 状態

COMPLETE

---

# Phase 58：Toda (5.3)

specialization:

```text
α=η₃
i=4
β=ν′
```

結果:

```text
ν′∈π_6^3
H(ν′)=η₅
2ν′=η₃∘η₄∘η₅
```

### 状態

COMPLETE

---

# Phase 59：Toda Proposition 5.3

finite-dimensional result:

```text
η_n²:=η_n∘η_(n+1)
π_(n+2)^n=Z/2{η_n²}
(n≥2)
```

### 状態

COMPLETE

---

# Phase 60：Toda Lemma 5.4

結果:

```text
ν₄∈π_7^4
H(ν₄)=ι₇
2Eν₄=E²ν′
```

Whitehead correction、Theorem 3.6 specialization、Toda (5.4) を接続。
`LiteratureStatement` を導入し、representative probe では literature usage と hand-authored proof-style derivation を表示。

### 状態

COMPLETE

---

# Phase 61：Toda Lemma 5.5

対象:

```text
β∈π_(t+2)(S^m)
β∘η_(t+2)=0
t>0
```

結果:

```text
{η_(m+2),E³β,η_(t+5)}_3
contains
±(E²β∘E^tν₄)
```

Phase 60 の α* / ν₄ provenance を再利用。

### 状態

COMPLETE

---

# Phase 62：ν-family / Toda (5.5)

定義:

```text
ν_n:=E^(n-4)ν₄
(n≥4)
```

有限次元 relation:

```text
2ν_n=E^(n-3)ν′
4ν_n=η_n³
(n≥5)
```

### 状態

COMPLETE

---

# Phase 63：Toda (5.6)

Proposition 4.4, n=4, α=ν₄ specialization:

```text
π_(i-1)^3 ⊕ π_i^7
≅
π_i^4

(α,β)↦Eα+ν₄∘β
```

### 状態

COMPLETE

---

# Phase 64：performance stabilization

数学 capability を変更せず regression performance を改善。

主な対応:

```text
deterministic fixture builder caching
premise-binding rematch elimination
crosscheck lattice precomputation
```

same-machine representative:

```text
259.11s
→
29.97s
```

### 状態

COMPLETE

---

# Phase 65：Toda Proposition 5.6 finite-dimensional

結果:

```text
π_5^2=Z/2{η₂³}
π_6^3=Z/4{ν′}
π_7^4=Z{ν₄}⊕Z/4{Eν′}
π_(n+3)^n=Z/8{ν_n}
(n≥5)
```

### 状態

COMPLETE

---

# Phase 66：Toda Equation (5.8)

結果:

```text
Δ(ι₉)=±(2ν₄-Eν′)=±[ι₄,ι₄]
```

formal proof record の運用を開始。

### 状態

COMPLETE

---

# Phase 67：Toda Lemma 5.7

結果:

```text
E²α∈2ι₅∘π_(i+2)(S⁵)
→
E(η₂∘α)=0
```

特に:

```text
E(η₂∘ν′)=0
π_6^2=Z/4{η₂ν′}
Δ(ν₅)=±η₂ν′
```

### 状態

COMPLETE

---

# Phase 68：Toda Proposition 5.8 finite-dimensional

結果:

```text
π_6^2=Z/4{η₂ν′}
π_7^3=Z/2{ν′η₆}
π_8^4=Z/2{ν₄η₇}⊕Z/2{Eν′η₇}
π_9^5=Z/2{ν₅η₈}
π_(n+4)^n=0
(n≥6)
```

supporting relations:

```text
Δ(η₉)=Eν′η₇
η₃ν₄=ν′η₆
η_nν_(n+1)=0
ν_nη_(n+3)=0
```

### 状態

COMPLETE

---

# Phase 69：Toda Equation (5.10)

結果:

```text
Δ(ι₁₁)=ν₅η₈
```

### 状態

COMPLETE

---

# Phase 70：Toda Proposition 5.9 finite-dimensional

結果:

```text
π_7^2=Z/2{η₂ν′η₆}
π_8^3=Z/2{ν′η₆²}
π_9^4=Z/2{ν₄η₇²}⊕Z/2{Eν′η₇²}
π_10^5=Z/2{ν₅η₈²}
π_11^6=Z{Δι₁₃}
π_(n+5)^n=0
(n≥7)
```

### 状態

COMPLETE

---

# Phase 71：Toda Equation (5.12)

concrete injectivity:

```text
Δ:π_11^9→π_9^4
Δ:π_12^11→π_10^5
Δ:π_13^13→π_11^6
```

for `n=4,5,6`.

### 状態

COMPLETE

---

# Phase 72 / 72R：Toda Lemma 5.10

canonical result:

```text
Δ(ι₁₃)
∈
{ν₆,η₉,2ι₁₀}
mod 2π₁₁(S⁶)
```

Phase 72R で ordinary homotopy group と Toda (4.3) の `π_i^n` の表現境界を修正。

追加した canonical semantics:

```text
HomotopyGroup
ordinary Toda (2.11) exactness
Serre finite-group applicability
ordinary / 2-primary image bridge
composition-level primary reduction
Proposition 2.6 indexed bracket consequence
Toda (1.15) indexed-to-ordinary bridge
```

historical Phase 72 shortcut proof instance は canonical ancestry から退役。

### 状態

COMPLETE

---

# Phase 72R-A1：Toda (4.3) semantic audit

`TodaPrimaryGroup(i,n)` は一様な 2-primary group ではなく Toda の `π_i^n` を表すことを documentation / regression で固定。

```text
i=n
→ ordinary diagonal group

i=2n-1
→ exceptional E-preimage subgroup

otherwise
→ 2-primary component
```

Phase 66–71 の既存利用はこの semantics と整合することを確認。

### 状態

COMPLETE

---

# Phase 73：Toda Proposition 5.11 finite-dimensional

結果:

```text
π_8^2=Z/2{η₂ν′η₆²}
π_9^3=0
π_10^4=Z/8{ν₄²}
π_(n+6)^n=Z/2{ν_n²}
(n≥5)
```

definition:

```text
ν_n²:=ν_n∘ν_(n+3)
```

supporting Toda (5.13):

```text
Δ(ν₉)=±2ν₄²
Δ(η₁₁²)=0
Δ(η₁₃)=0
```

### 状態

COMPLETE

---

# Phase 74：Toda Lemma 5.12

結果:

```text
{η_n,ν_(n+1),η_(n+4)}
=
{ν_n²}
(n≥6)
```

proof spine:

```text
bracket defined
→ indeterminacies vanish
→ singleton mod two
→ coefficient stability
+ n=8 nonzero anchor
→ coefficient = 1
```

### 状態

COMPLETE

---

# Phase 75：Toda Proposition 5.15 finite-dimensional

結果:

```text
π_9^2=0
π_10^3=0
π_11^4=0
π_12^5=Z/2{σ'''}
π_13^6=Z/4{σ''}
π_14^7=Z/8{σ'}
π_15^8=Z{σ₈}⊕Z/8{Eσ'}
π_(n+7)^n=Z/16{σ_n}
(n≥9)
```

Toda Lemma 5.13、Lemma 5.14、Toda (5.14)、Toda (5.15) を含む有限次元 σ-family branch を統合。

### 状態

COMPLETE

---

# Phase 76：Toda Equation (5.16)

結果:

```text
Ker(E:π_15^8→π_16^9)
=
Z{2σ₈-Eσ'}

Im(Δ:π_17^17→π_15^8)
=
Z{2σ₈-Eσ'}

Δ(ι₁₇)
=
±(2σ₈-Eσ')
```

### 状態

COMPLETE

---

# Phase 77：Toda Lemma 5.16

canonical source correction:

```text
printed E^nβ
→ typed / proof-text consistent E^7β
```

結果:

```text
E^4β∘σ_(t+8)
∈
(-1)^m x {ν_(m+4),E^7β,ν_(t+11)}_7
+
(-1)^t x {E^4β,ν_(t+8),2ν_(t+11)}_(t+3)
```

### 状態

COMPLETE

---

# Phase 78：stable G_0 through G_7

stable representation:

```text
StableHomotopyGroup
StablePrimaryComponent
```

最終結果:

```text
G_0=Z{ι}

(G_1;2)=Z/2{η}
(G_2;2)=Z/2{η²}
(G_3;2)=Z/8{ν}
(G_4;2)=0
(G_5;2)=0
(G_6;2)=Z/2{ν²}
(G_7;2)=Z/16{σ}
```

final aggregate:

```text
TodaStableG0ToG7Statement
ProofRule.INFERENCE
```

### 状態

COMPLETE

---

# Phase 79：minimal in-memory Proof Repository

追加:

```text
proof_repository.py
ProofRepositoryEntry
ProofRepository
```

API:

```text
register()
get()
entries()
find_by_conclusion()
find_by_statement_type()
find_by_phase()
find_by_theorem()
dependencies()
```

責務:

```text
registration
metadata
lookup
direct dependency access
```

非責務:

```text
proof construction
proof inference
proof rewriting
persistence
```

### 状態

COMPLETE

---

# Phase 80：repository-assisted automatic inference

追加:

```text
repository_available_steps()
find_goal_step()
RepositoryInferenceResult
derive_goal_from_repository()
```

代表 actual theorem:

```text
Toda Lemma 5.16
```

repository の既存 direct premises と explicit rule set から新しい final `ProofStep` を導出可能にした。

### 状態

COMPLETE

---

# Phase 81：automatic rule selection

追加:

```text
InferenceRuleCatalogEntry
InferenceRuleCatalog
find_goal_compatible_rule_entries()
find_goal_compatible_rules()
derive_goal_from_repository_with_catalog()
```

`fixed_point_safe=True` の opt-in rule のみ automatic selection 対象。

### 状態

COMPLETE

---

# Phase 82：one-level goal-directed producer search

final rule の premise が1つ不足した場合:

```text
goal
→ final rule
→ missing premise
→ unique producer
→ intermediate
→ final goal
```

を1段だけ自動生成。

### 状態

COMPLETE

---

# Phase 83：multiple one-level producers

複数 missing premise があっても、それぞれ producer が一意なら同一1段で複数 intermediate を生成し final goal まで導出。

### 状態

COMPLETE

---

# Phase 84：bounded depth=2 producer search

共有 dependency を含む producer DAG を表現。

```text
final
├─ bracket-sum
└─ composition
   └─ bracket-sum
```

同じ shared producer `ProofStep` を1回だけ生成して再利用。

### 状態

COMPLETE

---

# Phase 85：bounded-search diagnostics / integrated execution

追加:

```text
BoundedProducerSearchStatus
BoundedProducerSearchDiagnostic
BoundedProducerSearchReport
BoundedProducerExecutionResult
```

search failure と execution failure を区別し、diagnosed selected path と actual executed path を一致させる integrated API を追加。

### 状態

COMPLETE

---

# Phase 86：bounded-search depth parameterization

hard-coded depth=2 を explicit:

```text
max_depth
```

へ一般化。

formal regression:

```text
max_depth=2
max_depth=3
max_depth=4
```

depth=3 / 4 でも dependency-first execution と provenance / repository non-mutation を維持。

### 状態

COMPLETE

---

# Phase 87：finite producer retry

追加:

```text
FiniteProducerRetryPolicy(max_attempts=N)
```

default:

```text
retry_policy=None
```

では legacy `AMBIGUOUS_PRODUCER` を維持。

explicit retry budget がある場合のみ catalog order で有限 retry し、failed branch の temporary state を rollback。

### 状態

COMPLETE

---

# Phase 88：concrete theorem-instance producer compatibility

same conclusion type の複数 producer を concrete theorem instance で識別可能にした。

代表 collision:

```text
TodaDeltaImageUpToSignStatement

Phase 52 Δ(ι₅)
Phase 66 Δ(ι₉)
Phase 76 Δ(ι₁₇)
```

追加:

```text
InferenceRuleCatalogEntry.goal_compatibility
PremiseAvailability.bindings
MissingPremiseProducerLookup.requested_statement
BoundedProducerSearchNode.requested_statement
```

flow:

```text
same statement type
↓
known sibling bindings
↓
concrete requested_statement
↓
goal_compatibility
↓
false ambiguity elimination
↓
selected concrete request provenance
↓
execution output exact validation
```

最終 repository-wide regression:

```text
7096 passed in 35.90s
```

### 状態

COMPLETE

---

# Phase 89：post-Phase88 proof-search pressure / true-ambiguity necessity audit

Phase 88 後の producer search を actual theorem corpus で監査した。

## Phase 89-1：true ambiguity necessity audit

確認項目:

```text
same concrete requested_statement に
複数 safe producer が残る actual theorem instance があるか

Phase 88 filtering 後に
AMBIGUOUS_PRODUCER が actual theorem で残るか

finite retry だけでは不足し
deeper backtracking が必要な具体例があるか

producer registration order が
proof result を不適切に支配する actual example があるか
```

監査結論:

```text
現時点の actual theorem-backed corpus では、
Phase 88 concrete filtering 後に
同じ concrete requested statement へ
複数 viable producer が残る例は確認されていない。
```

したがって:

```text
general backtracking
producer ranking
proof-cost model
best-proof selection
DFS / BFS / A*
```

を Phase 89 で実装する根拠はないと判断。

Phase 87 finite retry は synthetic / defensive ambiguity handling として有効なので維持する。

production search algorithm:

```text
変更なし
```

### 状態

COMPLETE

---

# Phase 89 completion

結論:

```text
Phase 88 concrete filtering
→ actual false ambiguity を処理

Phase 87 finite retry
→ explicit true ambiguity handling の最小 fallback

general backtracking / ranking
→ 現時点では必要性未証明
```

次の方針を search algorithm expansion から query / calculation architecture へ移す。

### 状態

COMPLETE

---

# Phase 90：Toda group query / known-result lookup architecture

目的:

```text
input:
  n, k

target:
  π_(n+k)^n

↓
既存 theorem-backed group result を
ProofRepository から query できる基盤を作る
```

この Phase では新しい数学 theorem を導出しない。

また対象は一般の ordinary `π_(n+k)(S^n)` ではなく、現行数学実装に合わせて Toda の `π_(n+k)^n` とする。

---

## Phase 90-1：current calculation representation / orchestration audit

現行 representation を監査。

確認済み:

```text
target group
→ TodaPrimaryGroup

nonzero group result
→ Relation(
     lhs=TodaPrimaryGroup,
     rhs=FreeCyclicGroup
         | FiniteCyclicGroup
         | DirectSumGroup,
     relation_type=EQUALITY,
   )

zero group result
→ TodaPrimaryGroupZeroStatement

generator / order
→ RHS group structure object

EHP sequence / window
→ TodaEHPSequence / TodaEHPExactnessWindow

proof provenance
→ ProofStep.premises / inference_rule

repository metadata
→ ProofRepositoryEntry
```

不足:

```text
(n,k) query object
query validation
query → target construction
known-result lookup
recursive provenance extraction
normalized calculation result
human-readable report
```

Phase 90 は最初の2点:

```text
query representation
known-result lookup
```

だけを対象とすることを決定。

### 状態

COMPLETE

---

## Phase 90-2：TodaGroupQuery minimal representation

新規:

```text
toda_group_query.py
tests/test_phase90_minimal_group_query.py
```

追加:

```text
TodaGroupQuery(n,k)
```

validation:

```text
n は int
bool は拒否
n >= 1

k は int
bool は拒否
k >= 0
```

target:

```text
TodaGroupQuery(n,k).target
=
TodaPrimaryGroup(
  group_dimension=n+k,
  sphere_dimension=n,
)
```

責務境界:

```text
query validation
target construction
```

Phase 90-2 では行わない:

```text
repository lookup
proof search
group computation
presentation
```

focused:

```text
13 passed in 1.04s
```

repository-wide:

```text
7109 passed in 118.26s
```

### 状態

COMPLETE

---

## Phase 90-3-1：current known-result lookup semantics audit

確認した canonical result shape:

```text
nonzero:
Relation(
  lhs=target,
  rhs=FreeCyclicGroup
      | FiniteCyclicGroup
      | DirectSumGroup,
  relation_type=EQUALITY,
)

zero:
TodaPrimaryGroupZeroStatement(
  group=target,
)
```

`ProofRepository.find_by_conclusion()` は complete conclusion equality lookup なので、RHS を知らない query から group result を探す用途には直接使えない。

Phase 90-3 では generic `ProofRepository` を変更せず:

```text
repository.entries()
→ entry.step.conclusion
→ Toda-specific predicate
```

を採用。

semantics:

```text
GIVEN / INFERENCE を両方対象
0件 → ()
複数件 → 全件
registration order 保持
metadata は truth source にしない
proof search を起動しない
repository を変更しない
winner selection をしない
```

### 状態

COMPLETE

---

## Phase 90-3-2：minimal known-result predicate / lookup

新規:

```text
toda_group_lookup.py
tests/test_phase90_known_group_lookup.py
```

追加:

```text
is_toda_group_result_for_target()
find_known_toda_group_results()
```

lookup contract:

```text
find_known_toda_group_results(
  repository,
  query,
)
→ tuple[ProofRepositoryEntry, ...]
```

認識:

```text
FreeCyclicGroup
FiniteCyclicGroup
DirectSumGroup
TodaPrimaryGroupZeroStatement
```

除外:

```text
wrong target
non-EQUALITY relation
unsupported equality RHS
unrelated statement
```

`ProofRepository` 自身には Toda-specific API を追加しない。

関連回帰:

```text
50 passed in 5.89s
```

repository-wide:

```text
7125 passed in 97.71s
```

### 状態

COMPLETE

---

## Phase 90-3-3：actual theorem-backed known-result integration

production code:

```text
変更なし
```

追加:

```text
tests/test_phase90_actual_known_group_lookup.py
```

representative actual theorem-backed `ProofStep`:

```text
Phase 65:
π_7^4
=
Z{ν₄}⊕Z/4{Eν′}

Phase 73:
π_10^4
=
Z/8{ν₄²}

Phase 75:
π_9^2
=
0
```

queries:

```text
TodaGroupQuery(4,3)
→ actual π_7^4 ProofRepositoryEntry

TodaGroupQuery(4,6)
→ actual π_10^4 ProofRepositoryEntry

TodaGroupQuery(2,7)
→ actual π_9^2=0 ProofRepositoryEntry
```

verified:

```text
actual ProofStep identity preserved
actual premises preserved
wrong result cross-match なし
repository mutation なし
unregistered target → ()
```

関連回帰:

```text
108 passed in 8.67s
```

repository-wide:

```text
7135 passed in 101.39s
```

`git diff --check`:

```text
clean
```

### 状態

COMPLETE

---

# Phase 90 completion

最終 capability:

```text
(n,k)
↓
TodaGroupQuery
↓
TodaPrimaryGroup(n+k,n)
↓
ProofRepository
↓
existing theorem-backed group result lookup
↓
actual ProofRepositoryEntry
↓
actual ProofStep
```

Phase 90 で保証するもの:

```text
validated query input
exact Toda target construction
nonzero / zero group-result recognition
GIVEN / INFERENCE result lookup
multiple result preservation
actual theorem-backed ProofStep identity preservation
provenance preservation
repository non-mutation
lookup miss → empty tuple
```

Phase 90 でまだ行わないもの:

```text
group-result normalization
generator extraction
generator-order extraction
EHP exactness extraction
dependency classification
recursive proof provenance
derived / proved / imported / assumed classification
proof search on lookup miss
automatic calculation orchestration
human-readable report generation
```

final repository-wide regression:

```text
7135 passed in 101.39s
```

次:

```text
Phase 91-1
current group-result normalization /
generator-order extraction audit
```

### 状態

COMPLETE

---

# 現在の開発境界

最新の数学 frontier:

```text
stable G_0 through G_7
```

最新の search infrastructure:

```text
Phase 88 concrete theorem-instance filtering
Phase 89 necessity audit
```

最新の query infrastructure:

```text
Phase 90

(n,k)
→ target
→ existing theorem-backed group result
```

次の中期目標:

```text
query
→ structured calculation result
→ EHP/exactness extraction
→ mathematical dependency extraction
→ recursive provenance
→ orchestration
→ human-readable calculation report
```

---

# 文書運用方針

```text
README.md
= current capabilities / current status

docs/design.md
= current architecture / semantics / boundaries

docs/development_log.md
= chronological implementation history

docs/roadmap.md
= future capability dependency

docs/code_reference.md
= 主要ファイルの責務、主要 class / function、コード探索ガイド

docs/proof_records.md
= representative mathematical proofs
  + proof infrastructure / query infrastructure records
```

`development_log.md` は原則追記で運用する。
