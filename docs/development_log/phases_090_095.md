# Phase 90：π_(n+k)^n query / known-result lookup architecture

Phase 90 は、既存の Toda / EHP 数学と Proof Repository を user-facing calculation query へ接続する最初の Phase。

この Phase では新しい数学 theorem を追加しない。

正式な query target:

```text
π_(n+k)^n
```

であり、現時点では unrestricted ordinary:

```text
π_(n+k)(S^n)
```

全体を対象にしない。

---

## Phase 90-1：current calculation representation / orchestration audit

現行 GitHub code と関連 tests を確認。

既存 representation:

```text
TodaPrimaryGroup
TodaPrimaryGroupZeroStatement

FreeCyclicGroup
FiniteCyclicGroup
DirectSumGroup

TodaEHPSequence
TodaEHPExactnessWindow

Relation
ProofStep
ProofRepositoryEntry
ProofRepository
```

nonzero group result の canonical shape:

```text
Relation(
  lhs=TodaPrimaryGroup(...),
  rhs=FreeCyclicGroup(...)
      | FiniteCyclicGroup(...)
      | DirectSumGroup(...),
  relation_type=RelationType.EQUALITY,
)
```

zero group result:

```text
TodaPrimaryGroupZeroStatement(
  group=TodaPrimaryGroup(...),
)
```

generator / order は RHS group object に既に保持されている。

EHP exactness provenance も既存 theorem-backed proof では:

```text
TodaEHPExactnessWindow
→ exactness statement
→ derived map property / group result
```

として `ProofStep.premises` に残っている。

`ProofRepository` は:

```text
register
get
entries
find_by_conclusion
find_by_statement_type
find_by_phase
find_by_theorem
dependencies
```

を持つが recursive provenance walker は持たない。

不足:

```text
(n,k) query representation
query validation
query → target construction
target-only known-result lookup
normalized calculation result
recursive provenance extraction
fact classification
top-level orchestration
human-readable report
```

Phase 90 では:

```text
query representation
known-result lookup
```

までに scope を限定。

### 状態

COMPLETE

---

## Phase 90-2：TodaGroupQuery minimal representation

追加:

```text
toda_group_query.py
tests/test_phase90_minimal_group_query.py
```

representation:

```text
TodaGroupQuery(
  n: int,
  k: int,
)
```

validation:

```text
n:
  int
  bool rejected
  n >= 1

k:
  int
  bool rejected
  k >= 0
```

target:

```text
TodaGroupQuery(n,k).target
=
TodaPrimaryGroup(
  group_dimension=n+k,
  sphere_dimension=n,
)
```

責務:

```text
input validation
target construction
```

非責務:

```text
repository lookup
proof search
group computation
EHP extraction
presentation
```

focused:

```text
13 passed in 1.04s
```

repository-wide:

```text
7109 passed in 118.26s
```

### 状態

COMPLETE

---

## Phase 90-3-1：current known-result lookup semantics audit

確認:

```text
ProofRepository.find_by_conclusion()
```

は complete conclusion equality lookup なので、query 時点で RHS group structure をまだ知らない target-only lookup には直接使えない。

採用 semantics:

```text
TodaGroupQuery
↓
query.target
↓
repository.entries()
↓
entry.step.conclusion
↓
Toda group-result shape filter
```

truth source:

```text
entry.step.conclusion
```

metadata:

```text
key
phase
theorem
```

は mathematical truth 判定に使わない。

lookup 対象:

```text
ProofRule.GIVEN
ProofRule.INFERENCE
```

を両方許容。

結果:

```text
0件
→ ()

1件
→ one entry

複数件
→ 全件を registration order のまま返す
```

Phase 90-3 では行わない:

```text
winner selection
ranking
conflict resolution
proof search on miss
repository mutation
```

### 状態

COMPLETE

---

## Phase 90-3-2：minimal known-result predicate / lookup

追加:

```text
toda_group_lookup.py
tests/test_phase90_known_group_lookup.py
```

functions:

```text
is_toda_group_result_for_target()
find_known_toda_group_results()
```

認識する nonzero result:

```text
Relation(
  lhs=target,
  rhs=FreeCyclicGroup
      | FiniteCyclicGroup
      | DirectSumGroup,
  relation_type=EQUALITY,
)
```

認識する zero result:

```text
TodaPrimaryGroupZeroStatement(
  group=target,
)
```

除外:

```text
wrong target
non-EQUALITY relation
unsupported RHS
unrelated statement
```

generic `ProofRepository` は変更せず、Toda-specific lookup semantics を別 module に置いた。

関連回帰:

```text
50 passed in 5.89s
```

repository-wide:

```text
7125 passed in 97.71s
```

### 状態

COMPLETE

---

## Phase 90-3-3：actual theorem-backed known-result integration

production code:

```text
変更なし
```

追加:

```text
tests/test_phase90_actual_known_group_lookup.py
```

代表 actual theorem-backed results:

```text
Phase 65:
π_7^4
=
Z{ν₄}⊕Z/4{Eν′}

Phase 73:
π_10^4
=
Z/8{ν₄²}

Phase 75:
π_9^2
=
0
```

queries:

```text
TodaGroupQuery(4,3)
→ actual π_7^4 ProofRepositoryEntry

TodaGroupQuery(4,6)
→ actual π_10^4 ProofRepositoryEntry

TodaGroupQuery(2,7)
→ actual π_9^2=0 ProofRepositoryEntry
```

verified:

```text
actual ProofStep identity preserved
actual premises preserved
actual INFERENCE provenance preserved
cross-target result not returned
repository not mutated
unregistered target → ()
```

関連回帰:

```text
108 passed in 8.67s
```

repository-wide:

```text
7135 passed in 101.39s
```

`git diff --check`:

```text
clean
```

### 状態

COMPLETE

---

# Phase 90 completion

完成 capability:

```text
(n,k)
↓
TodaGroupQuery
↓
TodaPrimaryGroup(n+k,n)
↓
ProofRepository
↓
existing theorem-backed group-result lookup
↓
actual ProofRepositoryEntry
↓
actual ProofStep
```

保証:

```text
validated input
exact Toda target construction
free / finite cyclic / direct-sum recognition
zero-group recognition
GIVEN / INFERENCE result lookup
multiple matching results preserved
registration order preserved
actual ProofStep identity preserved
actual premise provenance preserved
lookup miss = ()
repository non-mutation
```

未実装:

```text
normalized group-result object
generator extraction
generator-order extraction
EHP / exactness extraction
mathematical dependency extraction
recursive proof provenance
derived / proved / imported / assumed classification
proof search on lookup miss
calculation orchestration
human-readable report
```

final repository-wide regression:

```text
7135 passed in 101.39s
```

next:

```text
Phase 91-1
current group-result normalization /
generator-order extraction audit
```

### 状態

COMPLETE
---

# Phase 91：group structure / generator result

Phase 90 で取得可能になった actual theorem-backed group result を、計算結果として machine-readable に正規化する。

target flow:

```text
TodaGroupQuery(n,k)
↓
known ProofRepositoryEntry
↓
ProofStep.conclusion
↓
group structure
↓
generators
↓
generator orders
```

Phase 91 では EHP extraction / dependency extraction / report generation を先取りしない。

---

## Phase 91-1：current group-result normalization / generator-order extraction audit

production code:

```text
変更なし
```

current GitHub code と actual theorem-backed tests を監査。

対象:

```text
Relation + FreeCyclicGroup
Relation + FiniteCyclicGroup
Relation + DirectSumGroup
TodaPrimaryGroupZeroStatement
```

確認:

```text
FreeCyclicGroup
→ generator を lossless に取得可能
→ finite order field は存在しない

FiniteCyclicGroup
→ generator
→ positive integer order

DirectSumGroup
→ summands tuple の順序を保持可能
→ summand ごとの generator / order を対応可能

TodaPrimaryGroupZeroStatement
→ generators=()
→ generator_orders=()
```

設計決定:

```text
free generator order
→ None

finite generator order
→ positive int

generator / order
→ parallel tuple

DirectSumGroup
→ summand 順序を保持

group_structure
→ normalized result に保持

source_entry
→ 保持

proof_step
→ actual object identity を保持

Relation.source / note
→ normalized result に複製しない
```

Phase 91-2 candidate:

```text
TodaGroupResult

target
group_structure
generators
generator_orders
source_entry
proof_step
```

### 状態

COMPLETE

---

## Phase 91-2：minimal normalized result representation

新規:

```text
toda_group_result.py
```

追加:

```text
TodaGroupStructure
TodaGroupResult
```

fields:

```text
target
group_structure
generators
generator_orders
source_entry
proof_step
```

order semantics:

```text
None
= infinite order

positive int
= finite order
```

zero semantics:

```text
group_structure=None
generators=()
generator_orders=()
```

invariants:

```text
target is TodaPrimaryGroup
group_structure is supported type or None
generators is tuple[Expression,...]
generator_orders is tuple[int|None,...]
len(generators) == len(generator_orders)
finite order > 0
bool is rejected as order
zero result cannot contain generators
source_entry is ProofRepositoryEntry
proof_step is ProofStep
proof_step is source_entry.step
```

追加 test:

```text
tests/test_phase91_minimal_group_result.py
```

focused:

```text
10 passed in 0.90s
```

Phase 90 + Phase 91-2 regression:

```text
49 passed in 6.00s
```

repository-wide:

```text
7145 passed in 107.78s
```

`git diff --check`:

```text
clean
```

### 状態

COMPLETE

---

## Phase 91-3：actual theorem-backed normalization integration

更新:

```text
toda_group_result.py
toda_group_lookup.py
```

追加:

```text
_extract_toda_group_generators_and_orders()
normalize_toda_group_result()
find_normalized_toda_group_results()
```

新規 test:

```text
tests/test_phase91_actual_group_normalization.py
```

flow:

```text
TodaGroupQuery
↓
find_known_toda_group_results()
↓
actual ProofRepositoryEntry
↓
normalize_toda_group_result()
↓
TodaGroupResult
```

代表 actual results:

```text
Phase 65:
π_7^4
=
Z{ν₄}⊕Z/4{Eν′}

↓
generators = (ν₄, Eν′)
generator_orders = (None, 4)
```

```text
Phase 73:
π_10^4
=
Z/8{ν₄²}

↓
generators = (ν₄²,)
generator_orders = (8,)
```

```text
Phase 75:
π_9^2
=
0

↓
group_structure = None
generators = ()
generator_orders = ()
```

verified:

```text
actual group_structure identity preserved
actual source_entry identity preserved
actual ProofStep identity preserved
actual premises / provenance preserved
repository not mutated
unregistered query → ()
```

focused:

```text
9 passed in 6.39s
```

Phase 90-91 regression:

```text
58 passed in 7.82s
```

repository-wide:

```text
7154 passed in 100.44s
```

`git diff --check`:

```text
clean
```

### 状態

COMPLETE

---

# Phase 91 completion

完成 capability:

```text
(n,k)
↓
TodaGroupQuery
↓
existing theorem-backed group result
↓
actual ProofRepositoryEntry
↓
actual ProofStep
↓
TodaGroupResult
↓
group_structure
generators
generator_orders
```

保証:

```text
free order = None
finite order = positive int
zero group = None + empty tuples
DirectSumGroup summand ordering preserved
source_entry identity preserved
ProofStep identity preserved
premise provenance preserved
lookup / normalization do not mutate repository
```

未実装:

```text
EHP sequence / exactness extraction
dependency extraction
recursive proof traversal
fact classification
proof search on lookup miss
top-level calculation orchestration
human-readable report
```

final repository-wide regression:

```text
7154 passed in 100.44s
```

next:

```text
Phase 92-1
current EHP sequence / exactness representation audit
```

### 状態

COMPLETE

---

# Phase 92：EHP sequence / exactness extraction

Phase 91 の `TodaGroupResult` を、既存 EHP structural / exactness provenance と接続した。

## Phase 92-1：current EHP representation audit

実装前監査で確認:

```text
TodaEHPSequence
TodaEHPExactnessWindow
TodaProp42ExactnessStatement
TodaPrimaryGroup
ProofStep
TodaGroupResult
```

既存 `TodaEHPSequence` / `TodaEHPExactnessWindow` は structural representation であり、exactness theorem / provenance とは分離されている。

actual `π_9^5` proof では:

```text
π_10^9 --Δ--> π_8^4 --E--> π_9^5
π_8^4  --E--> π_9^5 --H--> π_9^9
π_9^5  --H--> π_9^9 --Δ--> π_7^4
```

の3 exactness window が `TodaProp42ExactnessStatement` として derived されていることを確認。

### 状態

COMPLETE

---

## Phase 92-2：minimal EHP sequence / window result representation

追加:

```text
toda_ehp_result.py

TodaEHPExactnessWindowResult
TodaEHPSequenceResult
```

semantics:

```text
TodaEHPExactnessWindowResult
→ existing TodaEHPExactnessWindow を保持

TodaEHPSequenceResult
→ target
→ existing TodaEHPSequence
→ ordered window results
```

validation:

```text
target must occur in sequence.terms
window must be contiguous in sequence
windows must be tuple
window result identity preserved
sequence identity preserved
subset windows allowed
```

focused:

```text
15 passed in 2.78s
```

repository-wide:

```text
7169 passed in 104.18s
```

### 状態

COMPLETE

---

## Phase 92-3：actual theorem-backed EHP extraction integration

追加:

```text
toda_ehp_extraction.py

_collect_relevant_toda_ehp_windows()
_order_contiguous_toda_ehp_windows()
_build_toda_ehp_sequence()
extract_toda_ehp_sequence_result()
```

入口:

```text
TodaGroupResult.proof_step
```

final proof ancestry を identity-safe に辿り:

```text
TodaProp42ExactnessStatement
```

を抽出する。

actual `π_9^5` proof では当初テスト期待値を:

```text
E-H
H-Δ
```

の2 window としていたが、実際の final proof ancestry からは `Δ-E` も到達可能であることを regression が示した。

テスト期待値を実際の proof graph に合わせて修正し、最終 extraction:

```text
π_10^9
--Δ-->
π_8^4
--E-->
π_9^5
--H-->
π_9^9
--Δ-->
π_7^4
```

を確定。

修正後 related regression:

```text
44 passed in 7.25s
```

repository-wide:

```text
7179 passed in 97.33s
```

重要:

```text
実装 traversal は正しかった
誤りは initial test expectation
```

### 状態

COMPLETE

---

## Phase 92-4：connect known group structures to EHP terms

追加:

```text
toda_ehp_group_enrichment.py

TodaEHPGroupTermResult
TodaEHPGroupEnrichmentResult
connect_known_toda_group_results()
```

各 term:

```text
TodaPrimaryGroup
↓
TodaGroupQuery
↓
find_normalized_toda_group_results()
↓
tuple[TodaGroupResult, ...]
```

actual integration:

```text
π_10^9 -> unknown
π_8^4  -> known
π_9^5  -> known
π_9^9  -> unknown
π_7^4  -> known
```

unknown / zero distinction:

```text
unknown
→ group_results=()

known zero
→ TodaGroupResult(group_structure=None)
```

focused / related:

```text
75 passed in 8.34s
```

repository-wide:

```text
7191 passed in 92.43s
```

### 状態

COMPLETE

---

## Phase 92-5：exactness-use provenance integration

追加:

```text
toda_ehp_exactness_provenance.py

TodaEHPExactnessUseResult
TodaEHPExactnessUseProvenanceResult
_collect_reachable_proof_steps()
extract_toda_ehp_exactness_use_provenance()
```

各 exactness window:

```text
window_result
↓
actual TodaProp42ExactnessStatement ProofStep
↓
direct reachable consumer ProofSteps
```

validation:

```text
exactness_step must conclude TodaProp42ExactnessStatement
exactness_step.window is window_result.window
consumer must directly contain exactness_step in premises
uses must preserve EHP window order
```

actual example:

```text
H-Δ exactness
↓
hopf_zero_step direct consumer
```

generic dependency graph は導入しない。

focused / related:

```text
68 passed in 8.90s
```

repository-wide:

```text
7203 passed in 109.03s
```

`git diff --check`:

```text
clean
```

### 状態

COMPLETE

---

# Phase 92 完了境界

完成 flow:

```text
TodaGroupQuery
↓
theorem-backed ProofRepositoryEntry
↓
TodaGroupResult
↓
actual ProofStep ancestry
↓
TodaEHPSequenceResult
↓
known group enrichment for EHP terms
↓
exactness ProofStep / direct-consumer provenance
```

代表 EHP chain:

```text
π_10^9 --Δ--> π_8^4 --E--> π_9^5 --H--> π_9^9 --Δ--> π_7^4
```

追加しなかったもの:

```text
generic mathematical dependency graph
generic dependency role classification
recursive proof explanation
automatic proof narrative generation
persistent proof database
new mathematical theorem semantics
```

次:

```text
Phase 93
proof dependency extraction / explanation layer
```

---

# Phase 93：proof dependency extraction / explanation layer

Phase 92 で EHP-specific provenance を取得できるようになったため、Phase 93 では final theorem-backed result から実際に使われた mathematical dependency を generic に抽出し、role-aware な representative explanation result へ統合した。

新しい数学 theorem semantics は追加していない。

代表 actual theorem:

```text
π_9^5=Z/2{ν₅η₈}
```

を使用した。

---

## Phase 93-1：current dependency representation / traversal audit

実装前に current GitHub code と関連 tests を監査。

確認対象:

```text
ProofStep.premises
ProofStep.inference_rule
ProofRepositoryEntry
TodaGroupResult
toda_ehp_extraction.py
toda_ehp_exactness_provenance.py
actual Phase 68 π_9^5 proof graph
```

監査で確定:

```text
direct dependency
= root ProofStep.premises に直接現れる ProofStep

transitive dependency
= premise graph を再帰的に辿って到達する ProofStep

shared dependency
= 複数経路から同じ ProofStep identity へ到達する dependency
```

重要設計:

```text
statement equality
!= provenance identity
```

したがって dependency deduplication は conclusion equality ではなく `ProofStep` object identity を使う。

`ProofRepositoryEntry` の:

```text
key
phase
theorem
```

は provenance metadata であり mathematical role classification の truth source にはしない。

Phase 92 の traversal は reuse 可能な知見を持つが、Phase 93-1 では premature refactor を行わないと決定。

Phase 93 / 94 boundary:

```text
Phase 93
→ what dependencies were used

Phase 94
→ how each dependency was itself proved recursively
```

### 状態

COMPLETE

---

## Phase 93-2：minimal dependency-result representation

追加:

```text
toda_proof_dependency.py
tests/test_phase93_minimal_dependency_result.py
```

追加 class:

```text
TodaProofDependency
TodaProofDependencyResult
```

initial representation:

```text
TodaProofDependency
  proof_step
  depth

TodaProofDependencyResult
  root_step
  dependencies
```

`is_direct` は stored field にせず:

```text
depth == 1
```

から derived property とした。

validation:

```text
proof_step must be ProofStep
depth must be int and not bool
depth > 0

root_step must be ProofStep
dependencies must be tuple
root_step must not appear in dependencies
same ProofStep identity must not appear twice
```

equal-but-distinct `ProofStep` object は distinct dependency として許可。

Phase 93-2 では traversal / role classification をまだ実装しない。

focused:

```text
16 passed in 2.43s
```

Phase 92 + Phase 93-2 regression:

```text
38 passed in 6.51s
```

repository-wide:

```text
7219 passed in 104.72s
```

`git diff --check`:

```text
clean
```

### 状態

COMPLETE

---

## Phase 93-3：actual theorem-backed dependency extraction

更新:

```text
toda_proof_dependency.py
```

追加:

```text
extract_toda_proof_dependencies()
```

新規 test:

```text
tests/test_phase93_actual_dependency_extraction.py
```

traversal strategy:

```text
breadth-first search
```

理由:

```text
shared dependency の first visit
→ shortest depth
```

queue への追加は `ProofStep.premises` tuple order を維持。

visited:

```text
id(ProofStep)
```

で管理。

結果:

```text
root identity preserved
direct dependency depth=1
transitive dependency extracted
shared dependency identity-deduplicated
shortest depth preserved
stable BFS order preserved
non-ProofStep premise ignored
root excluded
cycle-safe
```

actual `π_9^5` で確認した reachable dependencies:

```text
actual EHP exactness steps
hopf_zero_step
suspension_surjective_step
other actual Phase 68 proof ancestry
```

synthetic minimum fixtures は:

```text
shortest shared depth
stable BFS order
non-ProofStep premise exclusion
```

の semantics 固定だけに使用。

focused Phase 93:

```text
32 passed in 8.50s
```

Phase 92 -> 93 regression:

```text
54 passed in 4.91s
```

repository-wide:

```text
7235 passed in 99.34s
```

`git diff --check`:

```text
clean
```

### 状態

COMPLETE

---

## Phase 93-4：dependency role classification

更新:

```text
toda_proof_dependency.py
```

追加:

```text
TodaProofDependencyRole
classify_toda_proof_step_role()
```

roles:

```text
EHP_EXACTNESS
EHP_WINDOW
GROUP_STRUCTURE
RELATION
ORDER
MAP_PROPERTY
DEFINITION
LITERATURE
OTHER
```

`TodaProofDependency` に:

```text
role
```

を追加。

backward compatibility:

```text
role=OTHER
```

を default としたため Phase 93-2 形式の constructor は維持。

classification principles:

```text
first-class conclusion type
RelationType
```

を使用。

文字列としての theorem name / phase name には依存しない。

representative classification:

```text
TodaProp42ExactnessStatement
→ EHP_EXACTNESS

TodaEHPExactnessWindow
→ EHP_WINDOW

TodaPrimaryGroup = group structure
→ GROUP_STRUCTURE

RelationType.ORDER
→ ORDER

generic Relation
→ RELATION

Delta injective / Hopf zero / suspension surjective
→ MAP_PROPERTY

TodaNuFamilyDefinitionStatement
→ DEFINITION

LiteratureStatement
→ LITERATURE

unreviewed statement
→ OTHER
```

actual `π_9^5` で:

```text
3 exactness steps
3 structural EHP windows
π_8^4 group result
Delta injectivity
Hopf zero
E surjectivity
Delta eta_9 relation
generator bridge
nu_5 definition
```

の role を確認。

focused Phase 93:

```text
49 passed in 7.29s
```

Phase 92 -> 93 regression:

```text
71 passed in 4.30s
```

repository-wide:

```text
7252 passed in 91.30s
```

`git diff --check`:

```text
clean
```

### 状態

COMPLETE

---

## Phase 93-5：representative explanation integration

新規:

```text
toda_explanation.py
tests/test_phase93_representative_explanation.py
```

追加:

```text
TodaRepresentativeExplanationResult
build_toda_representative_explanation()
```

integrated fields:

```text
group_result
ehp_result
exactness_provenance
dependency_result
```

flow:

```text
TodaGroupResult
↓
extract_toda_ehp_sequence_result()
↓
extract_toda_ehp_exactness_use_provenance()
↓
extract_toda_proof_dependencies()
↓
TodaRepresentativeExplanationResult
```

EHP result が存在する場合:

```text
exactness_provenance required
exactness_provenance.ehp_result is ehp_result
```

を identity で検証。

dependency root:

```text
dependency_result.root_step
is
group_result.proof_step
```

を要求。

追加 convenience:

```text
dependencies_for_role()
```

role filter は dependency order を維持する。

actual `π_9^5` integration で:

```text
TodaGroupResult
TodaEHPSequenceResult
TodaEHPExactnessUseProvenanceResult
TodaProofDependencyResult
```

が同一 actual proof graph を参照することを確認。

さらに role filter から:

```text
EHP exactness dependencies
group-structure dependency
map-property dependencies
relation dependencies
definition dependency
```

を取得可能になった。

focused Phase 93:

```text
66 passed in 7.61s
```

Phase 92 -> 93 integration:

```text
88 passed in 5.05s
```

repository-wide:

```text
7269 passed in 102.15s
```

`git diff --check`:

```text
clean
```

### 状態

COMPLETE

---

# Phase 93 completion

完成 capability:

```text
final theorem-backed result
↓
actual ProofStep ancestry
↓
identity-safe dependency extraction
↓
shortest dependency depth
↓
direct / transitive distinction
↓
dependency role classification
↓
EHP sequence / exactness-use provenance integration
↓
TodaRepresentativeExplanationResult
```

representative target:

```text
π_9^5=Z/2{ν₅η₈}
```

representative machine-readable explanation components:

```text
group structure
generators / orders
EHP sequence
exactness ProofSteps
exactness consumers
known group dependency
map-property dependency
relation dependency
definition dependency
other actual proof dependencies
```

important boundary:

```text
Phase 93
= flat dependency view + role-aware integration

Phase 94
= recursive proof provenance / DAG
```

まだ実装していない:

```text
recursive first-class proof-node / edge graph
derived / imported / assumed status model
natural-language proof narration
best-proof selection
proof ranking
persistent proof graph database
```

final repository-wide regression:

```text
7269 passed in 102.15s
```

next:

```text
Phase 94-1
current recursive provenance / DAG representation audit
```

### 状態

COMPLETE

---

# Phase 94：recursive proof provenance

Phase 94 は、Phase 93 の flat dependency view を、shared-node-preserving な recursive proof graph へ拡張する infrastructure Phase。

新しい Toda theorem を証明する Phase ではない。

代表 target は引き続き:

```text
π_9^5=Z/2{ν₅η₈}
```

であり、Phase 68 の actual theorem-backed final `ProofStep` を root として利用する。

## Phase 94-1：current recursive provenance / DAG representation audit

実装前に確認:

```text
ProofStep.premises
TodaProofDependency
TodaProofDependencyResult
extract_toda_proof_dependencies()
TodaRepresentativeExplanationResult
Phase 92 exactness provenance traversal
actual π_9^5 proof graph
```

確定した設計:

```text
node identity
= ProofStep object identity
```

structural equality ではない。

equal-but-distinct `ProofStep` は別 node とする。

edge semantics:

```text
parent_step
premise_step
premise_index
```

`premise_index` は ProofStep だけを filter した後の index ではなく、元の:

```text
parent_step.premises
```

における index を保持する。

Phase 93 の `depth` は:

```text
root からの shortest depth
```

という summary であり、recursive edge structure そのものではない。

non-`ProofStep` premise は graph node / edge にしない。

`derived / proved / imported / assumed` status taxonomy は actual need が確認できないため追加しない。

本体コード:

```text
変更なし
```

### 状態

COMPLETE

---

## Phase 94-2：minimal proof-node / edge representation

`toda_proof_dependency.py` に追加:

```text
TodaProofNode
TodaProofEdge
TodaRecursiveProofProvenanceResult
```

`TodaProofNode`:

```text
proof_step
shortest_depth
role
```

`TodaProofEdge`:

```text
parent_step
premise_step
premise_index
```

`TodaRecursiveProofProvenanceResult`:

```text
root_step
nodes
edges
```

主要 invariant:

```text
root node shortest_depth = 0
non-root node shortest_depth > 0
same ProofStep identity appears at most once in nodes
root appears exactly once
edge endpoints must appear in nodes
edge premise identity must match parent_step.premises[premise_index]
duplicate proof edge rejected
```

shared dependency は一つの node と複数 incoming edge で表現可能。

関連回帰:

```text
45 passed in 1.84s
```

repository-wide:

```text
7282 passed in 36.82s
```

Windows 上の適用 script で `toda_proof_dependency.py` が CRLF へ変わり `git diff --check` が `^M` を報告したが、LF normalization で修正した。

これは実装 semantics の不具合ではない。

### 状態

COMPLETE

---

## Phase 94-3：actual theorem-backed recursive provenance extraction

追加:

```text
extract_toda_recursive_proof_provenance()
```

入口:

```text
TodaGroupResult.proof_step
```

traversal:

```text
breadth-first
```

node deduplication:

```text
id(ProofStep)
```

edge は parent node を処理するとき、すべての `ProofStep` premise に対して追加する。

これにより shared node 自体は1回だけでも、複数 parent からの edge は失わない。

actual `π_9^5` で確認:

```text
root
is
Phase 68 final_step
```

さらに:

```text
recursive node identities
=
Phase 93 flat dependency identities
+
root identity
```

各 dependency について:

```text
node.shortest_depth
=
dependency.depth
```

を確認。

actual edge として:

```text
hopf_zero_step
→ delta_injective_step

hopf_zero_step
→ h_delta_exactness_step
```

および exactness step → structural window step を確認。

関連回帰:

```text
68 passed in 3.06s
```

repository-wide:

```text
7295 passed in 36.52s
```

`git diff --check`:

```text
clean
```

### 状態

COMPLETE

---

## Phase 94-4：shared-node / cycle / stable-order regression

production code:

```text
変更なし
```

synthetic graph regression のみ追加。

固定した semantics:

```text
shared node
→ node identity は1個
→ incoming edge は複数保持

node order
→ breadth-first
→ premise order preserving

edge order
→ parent traversal order
→ original premise_index order

cycle
→ traversal terminates
→ back-edge retained

self-cycle
→ traversal terminates
→ self-edge retained
```

`ProofStep` は frozen dataclass なので cycle fixture 構築時だけ test 内で:

```text
object.__setattr__()
```

を利用した。

これは production proof construction semantics を変更しない。

shared revisit と cycle revisit のための新規 enum / status は追加せず、graph structure 自体で区別する。

関連回帰:

```text
50 passed in 2.77s
```

repository-wide:

```text
7303 passed in 36.63s
```

`git diff --check`:

```text
clean
```

### 状態

COMPLETE

---

## Phase 94-5：representative explanation integration

`toda_explanation.py` を更新。

`TodaRepresentativeExplanationResult` に追加:

```text
recursive_provenance:
  TodaRecursiveProofProvenanceResult
```

builder:

```text
build_toda_representative_explanation()
```

から:

```text
extract_toda_recursive_proof_provenance()
```

を呼び、existing structured explanation へ統合。

追加 invariant:

```text
recursive_provenance.root_step
is
group_result.proof_step
```

既存 invariant と合わせて:

```text
dependency_result.root_step
is recursive_provenance.root_step
is group_result.proof_step
```

を保証する。

Phase 93 の:

```text
dependencies_for_role()
```

API は維持。

関連回帰:

```text
48 passed in 2.98s
```

repository-wide:

```text
7313 passed in 36.98s
```

`git diff --check`:

```text
clean
```

### 状態

COMPLETE

---

## Phase 94 completion

Phase 94 で完成:

```text
ProofStep identity-based proof nodes
first-class parent -> premise edges
original premise_index preservation
BFS shortest depth
stable node order
stable edge order
shared-node preservation
multiple incoming edges
cycle-safe traversal
cycle back-edge preservation
self-cycle regression
actual theorem-backed π_9^5 recursive extraction
flat / recursive root identity integration
TodaRepresentativeExplanationResult integration
```

追加しなかったもの:

```text
derived / imported / assumed status taxonomy
automatic natural-language proof narration
proof ranking
best-proof selection
general graph database
top-level calculation orchestration
Phase 95 lookup/search policy
Phase 96 presentation layer
```

最終全体回帰:

```text
7313 passed in 36.98s
```

`git diff --check`:

```text
clean
```

### 状態

COMPLETE

---

# Phase 94 完了境界

現在:

```text
TodaGroupResult
+
EHP context
+
exactness-use provenance
+
flat dependency view
+
recursive proof DAG
+
representative structured explanation
```

まで machine-readable に取得可能。

次:

```text
Phase 95-1
current calculation entry points / orchestration boundary audit
```

まず実装せず:

```text
TodaGroupQuery
known-result lookup
TodaGroupResult
build_toda_representative_explanation()
ProofRepository
bounded proof search
```

の責務と接続境界を監査する。

# Phase 95 completion record：calculation orchestration

Phase 95 の主題は、Phase 90–94 で構築した theorem-backed query / normalization / EHP provenance / proof provenance を、一つの structured calculation entry point に束ねることだった。

Phase 95 では、将来の presentation layer を先取りせず、machine-readable calculation orchestration に限定して実装した。

---

## Phase 95-1：orchestration boundary audit

確認した current flow:

```text
TodaGroupQuery
↓
theorem-backed lookup
↓
TodaGroupResult
↓
EHP / exactness provenance
↓
flat dependencies
↓
recursive provenance
```

確定した原則:

```text
既存 Phase 90–94 API の上に orchestration layer を追加する
既存 object identity を保持する
multiple theorem-backed matches を silent selection しない
presentation は Phase 96 に分離する
```

lookup miss 時の bounded proof search は、その時点では concrete goal RHS が存在しないため直接接続しないことを確認した。

---

## Phase 95-2：minimal calculation result representation

追加:

```text
TodaCalculationStatus
TodaCalculationCandidate
TodaCalculationResult
```

status:

```text
NOT_FOUND
FOUND
MULTIPLE_RESULTS
```

cardinality semantics:

```text
0 candidates
→ NOT_FOUND

1 candidate
→ FOUND

2+ candidates
→ MULTIPLE_RESULTS
```

---

## Phase 95-3：known-result orchestration

追加:

```text
build_known_toda_calculation_result()
```

flow:

```text
TodaGroupQuery
↓
find_normalized_toda_group_results()
↓
build_toda_representative_explanation()
↓
TodaCalculationCandidate
↓
TodaCalculationResult
```

direct theorem-backed result の identity / provenance を保持した。

---

## Phase 95-4〜95-7：lookup-miss gap / goal-discovery audits

監査で次を確定した。

```text
TodaGroupQuery
↓
TodaPrimaryGroup target
↓
[large gap]
concrete normalizable group-result goal
↓
bounded proof search
```

target-only query から unknown RHS を持つ concrete proof goal を安全に一般生成する機構は存在しない。

一方、finite-dimensional aggregate theorem には既存の concrete branch statement が存在する。

そのため Phase 95 fallback は:

```text
lookup miss
↓
aggregate theorem
↓
existing concrete branch statement
```

を使う設計とした。

generic dataclass recursive scan、symbolic higher-range instantiation、generic target-to-goal generation は導入しない。

---

## Phase 95-8：calculation goal representation

追加:

```text
TodaCalculationGoalDiscoveryStatus
TodaCalculationGoalSource
TodaCalculationGoalCandidate
TodaCalculationGoalDiscoveryResult
```

`TodaCalculationGoalSource` は:

```text
source_entry
branch_name
```

を保持する。

nested branch は dotted path を許す。

---

## Phase 95-9：concrete aggregate branch extraction

追加:

```text
toda_calculation_goal_extraction.py
```

対象は explicit supported aggregate type / concrete branch のみ。

対象例:

```text
Toda Proposition 5.6
Toda Proposition 5.8
Toda Proposition 5.11
Toda Proposition 5.15
```

branch statement object identity を保持する。

---

## Phase 95-10：repository-wide goal discovery

追加:

```text
discover_concrete_toda_calculation_goal_candidates()
```

semantics:

```text
repository registration order
↓
supported aggregate entries
↓
matching concrete branches
↓
all candidates preserved
```

deduplication / ranking / silent selection は行わない。

---

## Phase 95-11〜95-12：direct precedence / existing branch recovery audit

確定した orchestration precedence:

```text
direct lookup
├─ resultあり
│  → direct result を返す
│
└─ miss
   → aggregate fallback
```

aggregate theorem の branch proof は aggregate `ProofStep.premises` に existing `ProofStep` として存在することを確認した。

新しい branch proof を再生成せず、original `ProofStep` を回収する設計とした。

---

## Phase 95-13：existing branch ProofStep recovery

追加:

```text
recover_toda_calculation_goal_candidate_steps()
```

semantics:

```text
branch dotted path を解決
↓
branch statement を取得
↓
premise.conclusion == branch statement
↓
original ProofStep を回収
```

premise index は hard-code しない。

同一 object の重複は identity deduplication する。

equal-but-distinct `ProofStep` は複数候補として保持する。

---

## Phase 95-14：normalization bridge audit

`normalize_toda_group_result()` は `ProofRepositoryEntry` を要求する。

recovered branch は top-level repository entry ではないため、normalization adapter として ephemeral `ProofRepositoryEntry` を使えることを確認した。

重要境界:

```text
ephemeral entry
→ repository に register しない

ephemeral_entry.step
is original recovered branch ProofStep
```

---

## Phase 95-15：recovered branch normalization

追加:

```text
normalize_recovered_toda_calculation_goal_candidate()
```

結果:

```text
TodaCalculationGoalCandidate
↓
existing branch ProofStep
↓
ephemeral ProofRepositoryEntry
↓
TodaGroupResult
```

保持:

```text
TodaGroupResult.proof_step
is original recovered branch ProofStep
```

multiple recovered steps は全件保持する。

Phase 95-15 completion regression:

```text
82 passed in 3.42s
repository-wide: 7395 passed in 36.98s
git diff --check: clean
```

---

## Phase 95-16：lookup-miss integration audit

aggregate fallback の最終 flow を確定した。

```text
direct lookup miss
↓
aggregate discovery
↓
recovery
↓
normalization
↓
explanation
↓
TodaCalculationResult
```

この監査で、aggregate provenance を final candidate に残す representation gap を確認した。

---

## Phase 95-17：aggregate provenance representation

`TodaCalculationCandidate` に:

```text
goal_source: TodaCalculationGoalSource | None
```

を追加した。

direct result:

```text
goal_source = None
```

aggregate-derived result:

```text
goal_source
→ original aggregate ProofRepositoryEntry
→ branch_name
```

一方:

```text
group_result.source_entry
→ ephemeral normalization entry

group_result.proof_step
→ original branch ProofStep
```

を維持する。

Phase 95-17 completion:

```text
focused / related: 89 passed in 4.23s
repository-wide: 7402 passed in 37.93s
git diff --check: clean
```

---

## Phase 95-18：top-level calculation orchestration

追加:

```text
build_toda_calculation_result()
```

flow:

```text
TodaGroupQuery
↓
direct theorem-backed lookup
├─ FOUND / MULTIPLE_RESULTS
│  → direct result
│
└─ NOT_FOUND
   ↓
   aggregate discovery
   ↓
   branch recovery
   ↓
   normalization
   ↓
   explanation
   ↓
   TodaCalculationResult
```

原則:

```text
direct result precedence
no aggregate fallback after direct hit
all aggregate candidates preserved
all recovered results preserved
no silent selection
repository non-mutation
```

Phase 95-18 completion:

```text
focused / related: 97 passed in 3.61s
repository-wide: 7410 passed in 37.50s
git diff --check: clean
```

---

## Phase 95-19：actual representative coverage audit

concrete aggregate branch の current coverage を監査した。

代表:

```text
π_7^4
π_9^5
π_10^4
π_11^5
π_9^2
π_12^5
```

確認 capability:

```text
direct sum
finite cyclic group
zero group
outer aggregate branch
nested aggregate branch
generator / order
original branch identity
aggregate provenance
EHP provenance
recursive provenance
```

symbolic higher-range branch は intentional boundary とした。

---

## Phase 95-20：actual representative top-level regression

actual aggregate entries だけを repository に登録し、top-level API だけから representative six targets を取得する regression を追加した。

対象:

\[
\pi_7^4,\quad
\pi_9^5,\quad
\pi_{10}^4,\quad
\pi_{11}^5,\quad
\pi_9^2,\quad
\pi_{12}^5.
\]

確認:

```text
group structure
generator orders
zero-group semantics
original branch ProofStep identity
aggregate source identity
nested branch path
π_9^5 EHP / exactness provenance
flat dependency root identity
recursive provenance root identity
repository non-mutation
```

completion:

```text
focused / related: 106 passed in 4.40s
repository-wide: 7419 passed in 38.11s
git diff --check: clean
```

---

## Phase 95-21：completion / remaining-gap audit

Phase 95 の implementation capability は完了していると判定した。

deferred:

```text
symbolic higher-range theorem instantiation
target-only unknown-RHS goal generation
target-only bounded-search fallback
detailed calculation failure taxonomy
proof ranking / best-proof selection
unbounded search
odd-primary full integration
all-primary ordinary sphere-homotopy calculation
```

これらは current concrete orchestration の correctness gap ではなく、独立した将来 capability とした。

---

## Phase 95-22：documentation synchronization

Phase 95 完了に合わせて documentation を整理した。

### Phase 95-22A

文書構造 audit。

肥大化した:

```text
development_log.md
proof_records.md
```

を archive 分割する方針を確定。

### Phase 95-22B

`development_log.md` と `proof_records.md` を索引化し、既存全文を archive に exact slice として保存した。

development archive:

```text
docs/development_log/phases_001_048.md
docs/development_log/phases_049_064.md
docs/development_log/phases_065_078.md
docs/development_log/phases_079_089.md
docs/development_log/phases_090_095.md
```

proof record archive:

```text
docs/proof_records/early_records_and_toda_5_8.md
docs/proof_records/toda_5_7_to_5_10.md
docs/proof_records/toda_5_11_to_5_16.md
docs/proof_records/stable_stems_g0_g7.md
docs/proof_records/proof_infrastructure_079_089.md
docs/proof_records/calculation_provenance_090_095.md
```

archive reconstruction SHA256:

```text
development_log:
fd0ea36749b08a4ab7f956244d400f620e7105f8f23b9c474edb3a881824bf97

proof_records:
406f549b04d1ae838afbee54e030617515d035d8e25bf037debcec34ce28ce5f
```

### Phase 95-22C

次を current-state document として全面整理した。

```text
README.md
docs/design.md
docs/roadmap.md
```

役割:

```text
README
→ concise current capability

design
→ current architecture / semantics / invariants

roadmap
→ future-oriented plan
```

古い Phase 94-era future statements を current docs から除去した。

### Phase 95-22D

本 completion record を development / proof archive に追記する。

### Phase 95-22E

document links / consistency / final verification を行い、Phase 95 を正式 COMPLETE とする。

---

## Phase 95 final architecture

Phase 95 completion 時点の structured calculation flow:

```text
(n,k)
↓
TodaGroupQuery
↓
direct theorem-backed lookup
├─ direct result
│  ↓
│  TodaGroupResult
│
└─ lookup miss
   ↓
   aggregate theorem discovery
   ↓
   concrete branch extraction
   ↓
   original branch ProofStep recovery
   ↓
   ephemeral-entry normalization
   ↓
   TodaGroupResult
↓
TodaRepresentativeExplanationResult
↓
TodaCalculationCandidate
↓
TodaCalculationResult
```

provenance separation:

```text
goal_source
→ original aggregate entry + branch path

group_result.source_entry
→ direct original entry
  or ephemeral normalization adapter

group_result.proof_step
→ actual theorem-backed / recovered branch ProofStep

dependency_result.root_step
→ same ProofStep

recursive_provenance.root_step
→ same ProofStep
```

---

## Phase 95 completion boundary

Phase 95 で完成したもの:

```text
top-level Toda calculation result representation
direct theorem-backed orchestration
aggregate concrete-branch discovery
repository-wide goal candidate discovery
original branch proof recovery
recovered branch normalization
aggregate provenance preservation
direct-result precedence
multiple-result preservation
EHP / exactness explanation integration
flat / recursive proof provenance integration
actual representative end-to-end regression
repository non-mutation
```

Phase 96 に送るもの:

```text
human-readable explanation
proof report
Markdown / console / LaTeX presentation
structured result の human-facing formatting
```

Phase 95 implementation capability は COMPLETE。

formal Phase completion は Phase 95-22E の document consistency / final verification 後に確定する。
